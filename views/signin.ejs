<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("./partials/head.ejs") %>
        <title>Sign In - Blogify</title>
    </head>
    <body
        class="min-h-screen bg-gradient-to-br from-gray-900 via-gray-900 to-gray-900 text-white"
    >
        <%- include("./partials/nav.ejs") %>

        <div class="container mx-auto px-4 pt-24 pb-12">
            <div class="max-w-md mx-auto">
                <div class="text-center mb-8" data-aos="fade-up">
                    <h1
                        class="text-4xl font-bold mb-4 bg-gradient-to-r from-blue-400 to-teal-400 bg-clip-text text-transparent"
                    >
                        Welcome Back
                    </h1>
                    <p class="text-gray-300">
                        Sign in to continue your blogging journey
                    </p>
                </div>

                <div
                    class="glass-effect rounded-2xl p-8 shadow-2xl"
                    data-aos="fade-up"
                    data-aos-delay="200"
                >
                    <form action="/user/signin" method="post" class="space-y-6">
                        <div class="space-y-2">
                            <label
                                for="email"
                                class="block text-sm font-semibold text-purple-300"
                            >
                                <i class="fas fa-envelope mr-2"></i>Email
                            </label>
                            <input
                                type="email"
                                id="email"
                                name="email"
                                required
                                placeholder="your@email.com"
                                class="w-full p-4 bg-gray-800 bg-opacity-50 border border-gray-600 rounded-xl text-white placeholder-gray-400 focus:border-blue-400 focus:ring-2 focus:ring-blue-400 focus:outline-none transition-all duration-300"
                            />
                        </div>

                        <div class="space-y-2">
                            <label
                                for="password"
                                class="block text-sm font-semibold text-purple-300"
                            >
                                <i class="fas fa-lock mr-2"></i>Password
                            </label>
                            <input
                                type="password"
                                id="password"
                                name="password"
                                required
                                placeholder="••••••••"
                                class="w-full p-4 bg-gray-800 bg-opacity-50 border border-gray-600 rounded-xl text-white placeholder-gray-400 focus:border-blue-400 focus:ring-2 focus:ring-blue-400 focus:outline-none transition-all duration-300"
                            />
                        </div>

                        <button
                            type="submit"
                            class="w-full bg-gradient-to-r from-blue-500 to-teal-500 text-white py-4 rounded-xl font-semibold hover:from-blue-600 hover:to-teal-600 transition-all duration-300 transform hover:scale-105 glow"
                        >
                            <i class="fas fa-sign-in-alt mr-2"></i>Sign In
                        </button>
                    </form>

                    <div class="text-center mt-6">
                        <p class="text-gray-400">
                            Don't have an account?
                            <a
                                href="/user/signup"
                                class="text-purple-400 hover:text-purple-300 transition-colors duration-300 font-semibold"
                                >Sign Up</a
                            >
                        </p>
                    </div>
                </div>
            </div>
        </div>

        <%- include("./partials/scripts.ejs") %>
    </body>
</html>
